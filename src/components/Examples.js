const [controles, setControles] = useState([]);      // Lista de controles
const [editingId, setEditingId] = useState(null);   // ID en edición
const [isFormVisible, setIsFormVisible] = useState(false); // Mostrar/ocultar formulario
const [currentPage, setCurrentPage] = useState(1);  // Paginación


  const [nombre, setNombre] = useState("") // Lista de nombres
  const [editingId, setEditingId] = useState(null) // ID en edición
  const [currentPage, setCurrentPage] = useState(1) // Paginación
  const [isFormVisible, setIsFormVisible] = useState(false) // Mostrar/ocultar formulario

    const [registros, setRegistros] = useState([]) // Lista de registros
    const [editingId, setEditingId] = useState(null) // ID en edición
    const [currentPage, setCurrentPage] = useState(1) // Paginación
    const [isFormVisible, setIsFormVisible] = useState(false) // Mostrar/ocultar formulario

  const [bandas, setBandas] = useState([]) // Lista de bandas
  const [editingId, setEditingId] = useState(null) // ID en edición
  const [currentPage, setCurrentPage] = useState(1) // Paginación
  const [isFormVisible, setIsFormVisible] = useState(false) // Mostrar/ocultar formulario